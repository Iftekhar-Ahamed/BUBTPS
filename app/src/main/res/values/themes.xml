<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.BUBTPS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorPrimaryVariant">@color/my_light_primary_variant</item>
        <item name="colorAccent">@color/my_light_accent</item>
        <item name="android:statusBarColor">@color/my_light_status_bar</item>
        <item name="android:navigationBarColor">@color/my_light_navigation_bar</item>
    </style>

    <style name="Theme.BUBTPS" parent="Base.Theme.BUBTPS" />

</resources>
